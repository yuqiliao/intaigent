<section class="min-h-[80vh] flex items-center overflow-hidden relative">
  <div aria-hidden="true" class="absolute inset-0 grid grid-cols-2 -space-x-52 opacity-40">
    <div class="blur-[106px] h-56 bg-gradient-to-br from-green-400 to-blue-400"></div>
    <div class="blur-[106px] h-32 bg-gradient-to-r from-blue-300 to-white-300"></div>
  </div>
  <div class="container mx-auto px-4 py-12 z-10">
    <div class="flex flex-col md:flex-row items-center justify-between">
      <div class="md:w-[45%] space-y-8">
        <h1 class="text-6xl font-bold text-gray-900 leading-tight">
          Agri<span class="text-blue-600 animate-pulse">Connect</span>
        </h1>
        <p class="text-2xl text-gray-700 max-w-md">
          Empowering farmers with <span class="text-green-600 font-semibold">AI-driven insights</span> and support for sustainable agriculture
        </p>
        <a href="#chatbot" class="inline-block bg-green-600 text-white px-10 py-4 rounded-full font-semibold hover:bg-green-700 transition-all duration-300 text-lg shadow-lg hover:shadow-xl transform hover:-translate-y-1">
          Get Started
        </a>
      </div>
      <div class="md:w-[55%] flex justify-center relative mt-8 md:mt-0">
        <div class="absolute inset-0 bg-gradient-to-r from-green-200 to-blue-200 rounded-full filter blur-3xl opacity-40 animate-pulse"></div>
        <div class="relative z-10 w-full h-full">
          <img src="/images/agriconnect/flowchart-small.drawio.svg" alt="AgriConnect Flowchart" class="w-full h-full object-contain animate-float" />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  @keyframes blob {
    0%, 100% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(20px, -20px) scale(1.1); }
    50% { transform: translate(0, -40px) scale(1); }
    75% { transform: translate(-20px, -20px) scale(0.9); }
  }
  .animate-float { animation: float 6s ease-in-out infinite; }
  .animate-blob { animation: blob 7s infinite; }
  .animation-delay-2000 { animation-delay: 2s; }
</style>
